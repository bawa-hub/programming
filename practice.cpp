#include <bits/stdc++.h>
using namespace std;

vector<int> separateDigits(vector<int> &nums)
{
    vector<int> v;
    for (int i = 0; i < s.length(); i++)
    {
        v.push_back(arr[i]);
    }
    return v;
}

int main()
{
    string s;
    cin >> s;
    char arr[s.length() + 1];
    strcpy(arr, s.c_str());
}